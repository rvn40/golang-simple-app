apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: {{ .Values.service.metadata.labels.app }}
    app.kubernetes.io/managed-by: Helm
  name: {{ .Values.service.metadata.name }}
spec:
  type: {{ .Values.service.spec.type }}
  ports:
  - port: {{ .Values.service.spec.ports[0].port }}
    protocol: TCP
    targetPort: {{ .Values.service.spec.ports[0].port }}
  - port: {{ .Values.service.spec.ports[1].port }}
    protocol: TCP
    targetPort: {{ .Values.service.spec.ports[1].port }}
  selector:
    app: {{ .Values.service.metadata.labels.app }}
status:
  loadBalancer: {}